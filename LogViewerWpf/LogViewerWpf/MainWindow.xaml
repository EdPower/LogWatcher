<Window x:Class="LogViewerWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LogViewerWpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <DockPanel Grid.Column="0">
            <Label Content="Customer" DockPanel.Dock="Top"/>
            <ComboBox Name="comboBoxCustomers" DockPanel.Dock="Top" Width="200" Height="20" Margin="10" />
            <WrapPanel Margin="10" DockPanel.Dock="Top">
                <CheckBox Name="checkBoxTrace" Content="Trace" Margin="10" IsChecked="true"  />
                <CheckBox Name="checkBoxDebug" Content="Debug" Margin="10" IsChecked="true"   />
                <CheckBox Name="checkBoxInformation" Content="Info" Margin="10" IsChecked="true"   />
                <CheckBox Name="checkBoxWarning" Content="Warning" Margin="10" IsChecked="true" />
                <CheckBox Name="checkBoxError" Content="Error" Margin="10" IsChecked="true"   />
                <CheckBox Name="checkBoxCritical" Content="Critical" Margin="10" IsChecked="true" />
            </WrapPanel>
            <ListBox Name="listBoxEntries" Margin="10" DockPanel.Dock="Top" />
        </DockPanel>
        <DockPanel Grid.Column="1">
            
        </DockPanel>
    </Grid>
</Window>
